import Typed from 'typed.js';

const typed = new Typed('#heading-primary-sub', {
  strings: [
    'is a front-end engineer',
    'is a student at UWO',
    'is looking for an internship',
  ],
  typeSpeed: 75,
  backSpeed: 100,
  startDelay: 1200,
});
